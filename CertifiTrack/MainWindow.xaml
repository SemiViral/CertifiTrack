<metro:MetroWindow x:Class="CertifiTrack.MainWindow"
				   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
				   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
				   xmlns:local="clr-namespace:CertifiTrack"
				   xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
				   xmlns:model="clr-namespace:CertifiTrack.Model"
				   GlowBrush="{DynamicResource AccentColorBrush}"
				   BorderThickness="1"
				   mc:Ignorable="d"
				   Title="Death Certificate Status Updates" Height="550" Width="900"
				   ShowMaxRestoreButton="False"
				   Background="{x:Null}"
				   TextElement.FontWeight="Medium"
				   TextElement.FontSize="14"
				   FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
				   WindowState="Maximized" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
	<DockPanel Margin="0,0,0,0" DataContext="{StaticResource MainViewModel}" Background="White">
		<TextBlock x:Name="TextBlock" Margin="0" TextWrapping="Wrap" Text="Death Certificate Status" FontWeight="Normal"
				   FontSize="18.667" TextAlignment="Center" DockPanel.Dock="Top"
				   Padding="0,6,0,0" HorizontalAlignment="Center" VerticalAlignment="Center"
				   Style="{StaticResource TextBlockHeader}" />

		<UniformGrid Columns="5" Margin="10" DockPanel.Dock="Top">
			<TextBlock FontFamily="Roboto" FontSize="24" FontWeight="Normal" Text="DOD" VerticalAlignment="Top"
					   HorizontalAlignment="Center" Style="{StaticResource TextBlockHeader}" />
			<TextBlock Text="Name" VerticalAlignment="Top" HorizontalAlignment="Center" FontWeight="Normal"
					   FontFamily="Roboto" FontSize="24" Style="{StaticResource TextBlockHeader}" />
			<TextBlock Text="DC Type" HorizontalAlignment="Center" VerticalAlignment="Top" FontWeight="Normal"
					   FontFamily="Roboto" FontSize="24" Style="{StaticResource TextBlockHeader}" />
			<TextBlock Text="Approved" HorizontalAlignment="Center" VerticalAlignment="Top" FontWeight="Normal"
					   FontFamily="Roboto" FontSize="24" Style="{StaticResource TextBlockHeader}" />
			<TextBlock Text="Status" VerticalAlignment="Top" HorizontalAlignment="Center" FontWeight="Normal"
					   FontFamily="Roboto" FontSize="24" Style="{StaticResource TextBlockHeader}" />
		</UniformGrid>

		<ListBox x:Name="ListBoxCertificate" ItemsSource="{Binding Certificates}" Margin="10,5,10,0" Background="{x:Null}"
				 DockPanel.Dock="Top" BorderBrush="{x:Null}" Foreground="{x:Null}"
				 ItemContainerStyle="{StaticResource ListBoxItemNoHighlighting}" />

		<StatusBar x:Name="Status" Margin="0" Background="#FFD8D8D8" DockPanel.Dock="Top" VerticalAlignment="Bottom">
			<TextBlock x:Name="Filepath" Text="{Binding FilePath}" TextWrapping="Wrap" Foreground="#DD000000" Margin="0"
					   Padding="7,0,0,0" FontSize="10.667" />
		</StatusBar>
	</DockPanel>

	<Window.Resources>
		<DataTemplate DataType="{x:Type model:DeathCertificate}">
			<local:DeathCertificateUserControl />
		</DataTemplate>
	</Window.Resources>
</metro:MetroWindow>